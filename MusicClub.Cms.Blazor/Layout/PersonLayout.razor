@layout MainLayout

@inherits LayoutComponentBase

@implements IDisposable

@inject MemoryService MemoryService
@inject NavigationManager NavigationManager

<section class="layout">
    @if (NavigationManager.ToBaseRelativePath(NavigationManager.Uri) == "person")
    {
        <NavLink href="/" class="button">Home</NavLink>
    }
    else
    {
        <NavLink href="/person" class="button">Index</NavLink>
    }

    <h2>Person</h2>

    @Body
</section>

@code {
    public void Dispose() => MemoryService.DisposePersonData();
}