@inherits DataResultInfoBase<ActResult>

@if (DataResult is { } dataResult)
{
    <h4>@dataResult.Start?.ToLongDateString() @DataResult.Name</h4>

    @if (dataResult.Title is { Length: > 0 } title)
    {
        <p>@title</p>
    }

    <Image ImageResult="dataResult.ImageResult" />
}